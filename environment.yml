name: analytics_eda
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - build
  - setuptools
  - twine
  - wheel
  - python-dateutil>=2.8

  # install package, plus everything in [project.optional-dependencies.dev]
  - pip:
      - typing_extensions>=4.8
      - -e ".[dev]"
